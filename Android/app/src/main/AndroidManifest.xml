<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hci_gradjevinski.materijali">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="hci_gradjevinski.materijali.Helper.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="hci_gradjevinski.materijali.NavigationDrawerActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="hci_gradjevinski.materijali.LoginActivity" android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="hci_gradjevinski.materijali.MainActivity" android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="hci_gradjevinski.materijali.Activitys.DetaljiProizvodaActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="hci_gradjevinski.materijali.NavigationDrawerActivity" />

        </activity>

        <activity android:name="hci_gradjevinski.materijali.Activitys.KorpaActivity"
            android:theme="@style/AppTheme"
            android:label="Korpa"
            >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="hci_gradjevinski.materijali.NavigationDrawerActivity" />

        </activity>
        <activity android:name="hci_gradjevinski.materijali.Activitys.NarudzbeActivity"
            android:theme="@style/AppTheme"
            android:label="Narudžbe">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="hci_gradjevinski.materijali.NavigationDrawerActivity" />
        </activity>

    </application>

</manifest>